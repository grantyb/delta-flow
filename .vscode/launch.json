{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (demo diff)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}/demo.code-workspace",
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/out/**/*.js"
      ],
      "preLaunchTask": "npm: compile"
    }
  ]
}
